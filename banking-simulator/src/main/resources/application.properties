# Database Configuration - PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/vaultbank
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=root

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Custom Properties
banking.alert.email-enabled=true
banking.alert.low-balance-threshold=500.00
banking.reports.output-dir=reports/

# Spring Mail Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=bankingapp49@gmail.com
spring.mail.password=exezvmxjbunacpwx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Server Configuration
server.port=8081

# DevTools (Allows auto-restart)
spring.devtools.restart.enabled=true

# Actuator
management.endpoints.web.exposure.include=*
